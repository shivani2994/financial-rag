# Environment
.env
.venv/

# Python
__pycache__/
*.pyc

# Generated indexes and processed chunks (rebuildable from data/ + ingestion)
data/chroma/
data/processed/
data/bm25/

# Structured query logs (Module 7) -- regenerated by running the pipeline/harness
logs/

# Fast gate output (regenerated every run) -- eval/gate_thresholds.json is the
# committed config that drives it and stays tracked; this is just the result.
eval/fast_gate_result.json

# OS
.DS_Store

# data
data/**/*.pdf